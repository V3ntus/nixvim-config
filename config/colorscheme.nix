{
  colorschemes = {
    gruvbox = {
      enable = true;

      settings = {
        terminal_colors = true; 

        # Needed to keep terminal transparency, if any
        transparent_mode = false;
      };
    };
  };
}
